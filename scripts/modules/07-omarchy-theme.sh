#!/usr/bin/env bash
# The theme files and activation are declarative in home/home.nix.
# Keep this module as an explicit install-stage marker for module ordering.
log "Thunderstruck theme is managed by Home Manager"
